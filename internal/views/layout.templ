package views

import "github.com/FredericoBento/HandGame/internal/views/components"


templ Page(title string, headerTitle string, navlinks map[string]string, content templ.Component) {
  <!DOCTYPE HTML>
  @components.Head()
  <html>
    <body>
      @components.Header(headerTitle, navlinks)
      <div id="contents">      
        @content
      </div>

      @components.Footer()
    </body>
  </html>
}
